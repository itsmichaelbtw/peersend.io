# PeerSend WebSocket Server

This directory contains the WebSocket server implementation for PeerSend, a peer-to-peer file sharing application. The server facilitates communication between clients, enabling them to discover each other and exchange files.
