export namespace ComponentTypes {
  export namespace SessionProps {
    export interface Container {
      heading: string;
      alternative: {
        text: string;
        link: string;
        linkText: string;
      };
    }
  }
}
